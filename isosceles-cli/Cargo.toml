[package]
name = "isosceles-cli"
version = "0.0.1"
edition = "2024"

[dependencies]
ratatui = {workspace = true, features = ["serde"]}
color-eyre = {workspace = true}
ratatui-textarea = {workspace = true}
clap = {workspace = true, features = ["derive"]}
colored = {workspace = true}
isosceles-core = {workspace = true}